// The small falling pink/yellow/white petals (rendered small enough to
// read as drifting dots/particles) have been removed site-wide per
// request. Kept as a no-op so every call site needs no changes.
function DecorativePetals() {
  return null;
}

export default DecorativePetals;
